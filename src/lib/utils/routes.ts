export const ROUTES = {
  billing: '/app/billing',
  /** Top-up / checkout entry point linked from credit CTAs. */
  billingTopUp: '/app/billing?tab=buy',
  create: '/app/create',
  uploads: '/app/uploads',
} as const;
