export const ROUTES = {
  billing: '/app/billing',
  /** Top-up / checkout entry point linked from credit CTAs. */
  billingTopUp: '/app/billing/buy',
  create: '/app/create',
  uploads: '/app/uploads',
} as const;
