/**
 * Constants injected at build time via Vite `define`.
 * Source of truth: build-meta.js (wired in vite.config.ts and vitest.config.ts).
 */
declare const __APP_VERSION__: string;
declare const __BUILD_SHA__: string;
